# Notes (rendered from class scripts)

These pages are generated from `docs/notes/*.py` files using Jupytext → `.ipynb`,
then rendered by `mkdocs-jupyter`.

## Workflow
1. Edit the `.py` in Spyder (cells with `# %%` and `# %% [markdown]`)
2. Run `python tools/sync_notes.py` to update `.ipynb`
3. Commit + push
